import ReactDOM  from "react-dom";

import ReactComponent from "./components/ParentChildComponent";

ReactDOM.createRoot(document.getElementById("root")).render((
    <section>
        <ReactComponent></ReactComponent>
    </section>
))
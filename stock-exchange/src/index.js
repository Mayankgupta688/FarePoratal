import ReactDOM  from "react-dom";

import ReactComponent from "./components/WorkingWithStyles";

ReactDOM.createRoot(document.getElementById("root")).render((
    <section>
        <ReactComponent></ReactComponent>
    </section>
))